apiVersion: v1
kind: Service
metadata:
  name: {{ include "managers-service.fullname" . }}
  labels:
    {{- include "managers-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "managers-service.selectorLabels" . | nindent 4 }}
